export interface Room {
  name: string;
  description: string;
  GitHubRepo: string;
  tags: string;
}

export interface OriginalRoom {
  RoomId: string;
  userId: string;
  name: string;
  description: string;
  GitHubRepo: string;
  tags: string;
}
